import LoginPageContent from "@/components/LoginPageContent";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div className="loginBg h-screen w-screen flex items-center justify-center">
      
        <LoginPageContent />
    
    </div>
  );
};

export default page;
